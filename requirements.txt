# Dipendenze per il parser di log universale

# Core dependencies per anonimizzazione
presidio-analyzer>=2.2.33
presidio-anonymizer>=2.2.33
pyyaml>=6.0

# Spacy per Presidio (modello inglese)
spacy>=3.4.0
# Download con: python -m spacy download en_core_web_sm

# Per analisi NLP avanzata (opzionale)
# transformers>=4.21.0

# Per sviluppo e testing (opzionali):
# pytest>=7.0.0
# pytest-cov>=4.0.0

# Per preprocessing SNN (necessarie):
pandas>=1.5.0
numpy>=1.21.0

# Per formato Parquet (opzionale):
# pyarrow>=10.0.0

# Per analisi avanzata dei dati (opzionali):
# scikit-learn>=1.2.0
# matplotlib>=3.5.0
